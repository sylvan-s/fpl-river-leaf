# Intel review — Friday decision queue

Generated by `build_intel_review.py`, regenerate weekly — do not hand-edit. This is the working document for the `fpl-friday-intel-review` meeting.

Live gameweek: 1. **0 bite(s) awaiting a decision.**

**Nothing here has touched ROLE_INTEL.md's `setpieces`/`adjustments` fences, and nothing will until it is explicitly accepted below.** Accepting a bite is the only path a piece of community intel has into the numbers `build_squad.py`/`optimise_squad.py` actually score on.

*(queue is empty — nothing pending review)*
---

## Reply with your decisions

For each bite above, say **accept**, **reject**, or **defer** (defer = leave it in next week's queue, no action). Only accepted bites with a `field_affected` get written into ROLE_INTEL.md's machine-readable fence; accepted bites without one are still logged as a decision and added to ROLE_INTEL.md's narrative section as confirmed intel.

## Source reliability, full picture

See `SOURCE_RELIABILITY.md` for the complete per-source track record (all sources, not just those with a bite pending this week).

